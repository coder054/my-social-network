{{> delete-tweet-notification}}

<div class="col-md-6 col-md-offset-3">
  {{> tweets}}
</div>

<ul class="hidden">
  {{#each userAllView.following}}
  <li> {{this}} </li>
  {{/each}}
</ul>

{{> helpers}}
